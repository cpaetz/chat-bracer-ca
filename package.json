{
  "name": "bracer-chat",
  "version": "1.0.26",
  "description": "Bracer Systems client support chat",
  "author": "Bracer Systems Inc.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "icons": "node scripts/generate-icons.js",
    "build": "npm run icons && node scripts/prep-wincosesign.js && electron-builder --win --config electron-builder.config.js",
    "build:dir": "npm run icons && node scripts/prep-wincosesign.js && electron-builder --win --dir --config electron-builder.config.js"
  },
  "devDependencies": {
    "electron": "^28.3.3",
    "electron-builder": "^24.13.3"
  },
  "dependencies": {
    "koffi": "^2.15.2"
  }
}

{
  "name": "bracer-chat",
  "version": "1.1.0",
  "description": "Bracer Systems client support chat",
  "author": "Bracer Systems Inc.",
  "main": "src/main.js",
  "scripts": {
    "start": "electron .",
    "icons": "node scripts/generate-icons.js",
    "prebuild": "node -e \"require('fs').writeFileSync('electron-version.txt', require('electron/package.json').version)\"",
    "build": "npm run icons && npm run prebuild && node scripts/prep-wincosesign.js && electron-builder --win --config electron-builder.config.js",
    "build:dir": "npm run icons && npm run prebuild && node scripts/prep-wincosesign.js && electron-builder --win --dir --config electron-builder.config.js"
  },
  "devDependencies": {
    "electron": "^41.1.0",
    "electron-builder": "^26.0.0"
  },
  "dependencies": {
    "koffi": "^2.15.2"
  },
  "overrides": {
    "lodash": "^4.17.21"
  }
}
